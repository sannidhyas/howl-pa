---
description: Show Google Tasks — local queue + synced rows.
disable-model-invocation: false
---

Call the `howl_tasks` MCP tool from the `howl-pa` server. Present as a table: Status · Title · Due · Importance · List. Sort by status (needs_push first, then needsAction, then completed).
