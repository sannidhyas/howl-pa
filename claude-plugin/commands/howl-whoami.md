---
description: Show the Howl PA dashboard identity used by this MCP server.
disable-model-invocation: false
---

Call the `howl_whoami` MCP tool from the `howl-pa` server. Present `username` and `dashboard_url`.
