---
description: Show upcoming Calendar events.
argument-hint: '[hours]'
disable-model-invocation: false
---

Call the `howl_calendar` MCP tool from the `howl-pa` server. If `$ARGUMENTS` is a number, pass it as `hours` (default 48). Present as a table: Starts · Summary · Location · Attendees · Meet link.
