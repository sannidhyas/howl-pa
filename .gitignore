node_modules/
.env
.env.local
store/
*.log
.DS_Store
.tsbuildinfo

# Machine-local state — never commit
.planning/codebase/
.planning/intel/
.codex
.agents/
vendor/

# Howl PA runtime config (user-specific) — belt-and-suspenders so a
# stray write inside the repo can never leak credentials.
.claudeclaw/
howl-pa-*.tgz
google-token.json
howl.db
howl.db-*
claudeclaw.pid
howl-pa.pid
config/howl-pa/
.config/howl-pa/

# codex-corps runtime artifacts
.claude/.codex-locks/
.claude/codex-lessons.md
